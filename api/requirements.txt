# Lightweight requirements for Vercel serverless deployment
# Excludes: sentence-transformers, playwright, torch (too large for 250MB limit)
# Set EMBEDDING_PROVIDER=google or openai in Vercel env vars to avoid local embeddings

# Core
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# LLM
openai>=1.0.0
groq>=0.4.0
google-generativeai>=0.3.0

# Vector Stores (cloud - recommended for serverless)
pinecone-client>=3.0.0

# ChromaDB (local - only works in /tmp on Vercel, use Pinecone instead)
chromadb>=0.4.0

# Web utilities
requests>=2.31.0
beautifulsoup4>=4.12.0
aiohttp>=3.9.0
lxml>=4.9.0

# Database
pymongo>=4.6.0
dnspython>=2.4.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
PyPDF2>=3.0.0
pyyaml>=6.0.0
tqdm>=4.66.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP client
httpx>=0.25.0
